<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GetWeather" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>

        <item name="activityBgColor">@color/backgroundColorLight</item>
        <item name="cardBgColor">@color/cardBgColorLight</item>
        <item name="imgBackground">@color/imgBgColorLight</item>
        <item name="imgBorder">@color/imgBorderColorLight</item>

        <item name="rvBgColor">@color/rvColorLight</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/statusBarColorLight</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.GetWeather.SplashTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">#ececff</item>
    </style>
</resources>